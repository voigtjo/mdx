<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="utf-8" />
  <title>Dashboard – MDX Platform</title>
  <link rel="stylesheet" href="/public/styles.css" />
</head>
<body class="bg-gray-100">
  <div class="min-h-screen">
    <!-- Header -->
    <header class="bg-white border-b">
      <div class="max-w-5xl mx-auto px-6 py-4 flex items-center justify-between">
        <div class="flex items-center space-x-4">
          <a href="/mdx" class="text-sm text-blue-600 hover:underline">
            ← Zurück zur Übersicht
          </a>
          <div>
            <h1 class="text-xl font-semibold">MDX Forms – Dashboard</h1>
            <p class="text-xs text-gray-500">
              Eingeloggt als <span class="font-mono"><%= user.email %></span>
            </p>
          </div>
        </div>
        <nav class="space-x-4 text-sm">
          <a href="/dashboard" class="text-blue-600 font-medium">Dashboard</a>
          <a href="/users" class="text-gray-600 hover:underline">Users</a>
          <a href="/tenants" class="text-gray-600 hover:underline">Tenants</a>
          <a href="/logout" class="text-red-600 hover:underline">Logout</a>
        </nav>
      </div>
    </header>

    <!-- Content -->
    <main class="max-w-5xl mx-auto px-6 py-8">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <div class="bg-white rounded-xl shadow p-5">
          <h2 class="text-sm font-semibold text-gray-700 mb-2">Meine Aufgaben</h2>
          <p class="text-3xl font-bold text-blue-600"><%= stats.assignedCount %></p>
          <p class="text-xs text-gray-500 mt-1">Dir aktuell zugewiesene Formulardatensätze.</p>
          <a href="/mdx/tasks" class="inline-block mt-3 text-sm text-blue-600 hover:underline">
            Zu meinen Aufgaben →
          </a>
        </div>

        <div class="bg-white rounded-xl shadow p-5">
          <h2 class="text-sm font-semibold text-gray-700 mb-2">Offene Group-Aufgaben</h2>
          <p class="text-3xl font-bold text-amber-500"><%= stats.openOperatorCount %></p>
          <p class="text-xs text-gray-500 mt-1">Offene Aufgaben, für die du Operator bist.</p>
          <a href="/mdx/tasks" class="inline-block mt-3 text-sm text-blue-600 hover:underline">
            Offene Aufgaben sehen →
          </a>
        </div>

        <div class="bg-white rounded-xl shadow p-5">
          <h2 class="text-sm font-semibold text-gray-700 mb-2">MDX-Formulare</h2>
          <p class="text-3xl font-bold text-gray-800"><%= stats.formCount %></p>
          <p class="text-xs text-gray-500 mt-1">Anzahl der Formulardefinitionen in deinem Tenant.</p>
          <a href="/mdx" class="inline-block mt-3 text-sm text-blue-600 hover:underline">
            Formular-Übersicht öffnen →
          </a>
        </div>
      </div>

      <section class="bg-white rounded-xl shadow p-6">
        <h2 class="text-lg font-semibold mb-4">Kurzüberblick</h2>
        <ul class="text-sm text-gray-700 space-y-1">
          <li>• Du bist eingeloggt im Tenant: <span class="font-mono"><%= user.tenantId %></span></li>
          <li>• Rollen (Session): <span class="font-mono"><%= (user.roles || []).join(", ") || "—" %></span></li>
          <li>• Dieses Dashboard ist der spätere Einstiegspunkt für Admin-/Operator-/Editor-Workflows.</li>
        </ul>
      </section>
    </main>
  </div>
</body>
</html>
