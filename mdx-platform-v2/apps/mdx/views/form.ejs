<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title><%= doc.title || doc.slug %> – MDX Formular</title>
  <script src="https://unpkg.com/htmx.org@1.9.10"></script>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

  <header class="bg-white shadow-sm">
    <div class="max-w-3xl mx-auto px-6 py-4 flex items-center justify-between">
      <div>
        <h1 class="text-xl font-semibold"><%= doc.title || doc.slug %></h1>
        <p class="text-sm text-gray-500">
          Tenant-Formular aus MDX gerendert.
        </p>
      </div>
      <nav class="space-x-4 text-sm">
        <a href="/mdx" class="text-gray-600 hover:text-gray-900">Zurück zur Übersicht</a>
        <a href="/logout" class="text-red-600 hover:text-red-800">Logout</a>
      </nav>
    </div>
  </header>

  <main class="max-w-3xl mx-auto px-6 py-10">
    <div class="bg-white rounded-xl shadow p-6">
      <!-- hier kommt das von mdx-htmx-lib erzeugte Formular rein -->
      <div>
        <%- formHtml %>
      </div>

      <div id="result" class="mt-4"></div>
    </div>
  </main>
</body>
</html>
