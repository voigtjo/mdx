<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="utf-8" />
  <title>Tenants – MDX Platform</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
  <div class="max-w-5xl mx-auto py-10">
    <header class="flex items-center justify-between mb-8">
      <div>
        <h1 class="text-2xl font-bold">MDX Platform – Tenants</h1>
        <p class="text-sm text-gray-500">
          Eingeloggt als <span class="font-mono"><%= user.email %></span>
        </p>
      </div>

      <nav class="space-x-4 text-sm">
        <a href="/dashboard" class="text-gray-700 hover:underline">Dashboard</a>
        <a href="/users" class="text-gray-700 hover:underline">Users</a>
        <a href="/tenants" class="text-blue-600 font-medium hover:underline">Tenants</a>
        <a href="/logout" class="text-red-600 hover:underline">Logout</a>
      </nav>
    </header>

    <main class="bg-white rounded-xl shadow p-6">
      <div class="flex items-center justify-between mb-4">
        <h2 class="text-xl font-semibold">Tenants</h2>
        <button class="px-3 py-2 rounded-lg bg-gray-200 text-gray-700 text-sm cursor-not-allowed" disabled>
          Neuer Tenant (coming soon)
        </button>
      </div>

      <p class="text-sm text-gray-500 mb-4">
        Auflistung aller in der <span class="font-mono">platform.tenants</span>-Collection vorhandenen Tenants (nur lesend, MVP).
      </p>

      <% if (!tenants || tenants.length === 0) { %>
        <div class="p-4 rounded-lg bg-yellow-50 border border-yellow-200 text-sm text-yellow-900">
          Es sind noch keine Tenants in der <span class="font-mono">platform.tenants</span>-Collection vorhanden.
        </div>
      <% } else { %>
        <div class="overflow-x-auto">
          <table class="min-w-full text-sm">
            <thead>
              <tr class="text-left text-gray-600 border-b">
                <th class="py-2 pr-4">Tenant-ID</th>
                <th class="py-2 pr-4">Name</th>
                <th class="py-2 pr-4">Created</th>
              </tr>
            </thead>
            <tbody>
              <% tenants.forEach(t => { %>
                <tr class="border-b last:border-b-0">
                  <td class="py-2 pr-4 font-mono"><%= t.id || "" %></td>
                  <td class="py-2 pr-4 font-medium"><%= t.name || "" %></td>
                  <td class="py-2 pr-4 text-gray-600">
                    <%= t.createdAt ? new Date(t.createdAt).toLocaleString("de-DE") : "" %>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } %>
    </main>
  </div>
</body>
</html>
