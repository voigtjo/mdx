<%- include("../ui/layout.ejs", {
  user: user,
  body: `
    <div class="bg-white p-8 rounded-xl shadow-md mb-8">
      <h1 class="text-3xl font-bold mb-4">Welcome ${user.email}</h1>
      <p class="text-gray-600">This is your multi-tenant MDX platform dashboard.</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">

      <div class="bg-white p-6 rounded-xl shadow-md">
        <h2 class="text-xl font-semibold mb-2">Users</h2>
        <pre class="bg-gray-100 p-4 rounded text-sm">${JSON.stringify(users || [], null, 2)}</pre>
      </div>

      <div class="bg-white p-6 rounded-xl shadow-md">
        <h2 class="text-xl font-semibold mb-2">Tenants</h2>
        <pre class="bg-gray-100 p-4 rounded text-sm">${JSON.stringify(tenants || [], null, 2)}</pre>
      </div>

    </div>
  `
}) %>
